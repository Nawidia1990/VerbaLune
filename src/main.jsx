import React from "react";
import ReactDOM from "react-dom/client";
import VerbaLune from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VerbaLune />
  </React.StrictMode>
);
